import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const WidgetContent = styled.div``

PeriodTime.propTypes = {
    
};

function PeriodTime(props) {
    return (
        <WidgetContent>
            
        </WidgetContent>
    );
}

export default PeriodTime;