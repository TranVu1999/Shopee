import React from 'react';
import PropTypes from 'prop-types';


DateBox.propTypes = {
    
};

function DateBox(props) {
    return (
        <div>
            
        </div>
    );
}

export default DateBox;